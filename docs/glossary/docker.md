#### Container

A container is a runnable instance of an image.

It is a virtualized run-time environment where users can run applications.
These applications are run isolated from the underlying system.

#### Image

An image is a read-only template with instructions for creating a Docker container.
